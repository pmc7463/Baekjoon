#include <stdio.h>
#include <stdlib.h>

int main(void)
{
    int num, counter = 0, range, start, temp, temp2, sum2 = 0;
    int* sum;
    
    scanf("%d", &num);
    
    temp = num;

    while(temp != 0) {
        temp /= 10;
        counter++;
    }
    

    range = 9 * counter;
    start = num - range;
    temp2 = start;

    sum = (int*)malloc(sizeof(int) * counter);

    for (int k = start; k <= num; k++) {
        temp2 = k;
        sum2 = 0;
        if (k + sum2 != num) {
            for (int i = 0; temp2 != 0; i++) {
                sum[i] = temp2 % 10;
                temp2 /= 10;
                sum2 += sum[i];
            }
            
        }
        else {
            printf("최종값 %d\n", k);     
            break;
        } 

        if (k == num)
            printf("실패 0\n");

    }
    printf("range : %d\n", range);
    printf("start : %d\n", start);a
    printf("sum2 : %d\n", sum2);
    free(sum);
    return 0;
}